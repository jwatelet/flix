<% @movies.each do |movie| %>
  <section class="movie">
    <div class="summary">
      <h2>
        <a>
          <%= link_to movie.title, movie%>
        </a>
      </h2>
      <h3>
        <%= total_gross(movie) %>
      </h3>
      <p>
        <%= truncate(movie.description, length: 150, separator: ' ') %>
      </p>
    </div>
  </section>
<% end %>